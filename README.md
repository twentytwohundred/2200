# CLA signatures branch

This orphan branch holds the signature records collected by the CLA Assistant Lite GitHub Action (`.github/workflows/cla.yml` on `main`).

`cla.json` is the canonical store. Each signature record is appended by the action when an external contributor posts the sign-off phrase on a PR.

Do not push code here. Do not branch-protect this branch (the action needs write access).
