{
  "name": "@2200/discord-connector",
  "version": "0.1.0",
  "description": "Discord connector Extension for 2200 (per-Agent bot identity via discord.js)",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "gateway": "tsx src/gateway.ts"
  },
  "dependencies": {
    "discord.js": "^14.16.0",
    "undici": "^8.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.6.0"
  }
}
