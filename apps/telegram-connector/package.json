{
  "name": "@2200/telegram-connector",
  "version": "0.1.0",
  "description": "Telegram connector Extension for 2200 (per-Agent bot identity via the Telegram Bot API)",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "gateway": "tsx src/gateway.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.19.20",
    "tsx": "^4.22.4",
    "typescript": "^5.6.0"
  }
}
