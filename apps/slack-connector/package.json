{
  "name": "@2200/slack-connector",
  "version": "0.1.0",
  "description": "Slack connector Extension for 2200 (per-Agent bot identity via Socket Mode)",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "gateway": "tsx src/gateway.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.19.20",
    "tsx": "^4.22.4",
    "typescript": "^5.6.0"
  }
}
